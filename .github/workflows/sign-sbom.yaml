name: Generate and Sign SBOM

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      # 1️⃣ Checkout source code
      - name: Checkout repository
        uses: actions/checkout@v5

      # 2️⃣ Setup Docker Buildx
      - name: Setup Docker buildx
        uses: docker/setup-buildx-action@v3

      # 3️⃣ Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # 4️⃣ Build & push image — output digest
      - name: Build and push image by digest
        id: build-digest
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/sbomdemo
          outputs: type=image,name=${{ secrets.DOCKERHUB_USERNAME }}/sbomdemo,push-by-digest=true,name-canonical=true

      # 5️⃣ Extract digest
      - name: Get image digest
        id: digest
        run: echo "digest=${{ steps.build.outputs.digest }}" >> $GITHUB_OUTPUT
 
      # 6️⃣ Generate CycloneDX SBOM using Trivy Action
      - name: Generate SBOM (Trivy)
        uses: aquasecurity/trivy-action@b6643a29fecd7f34b3597bc6acb0a98b03d33ff8 # 0.33.1
        with:
          scan-type: image
          image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/sbomdemo@${{ steps.digest.outputs.digest }}
          format: "cyclonedx"
          output: "sbom.cyclonedx.json"

      # 7️⃣ Install Cosign
      - name: Install Cosign
        uses: sigstore/cosign-installer@v4.0.0

      # 8️⃣ Sign container image (SHA digest)
      - name: Sign image by digest
        env:
          COSIGN_EXPERIMENTAL: "true"
        run: |
          cosign sign --yes \
          ${{ secrets.DOCKERHUB_USERNAME }}/trivydemo@${{ steps.digest.outputs.digest }}
      
      # 9️⃣ Verify image signature
      - name: Verify image signature
        env:
          COSIGN_EXPERIMENTAL: "true"
        run: |
          cosign verify \
            ${{ secrets.DOCKERHUB_USERNAME }}/trivydemo@${{ steps.digest.outputs.digest }} \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            --certificate-identity-regexp "https://github.com/.*/actions/runs/.*"

      # 🔟 Sign SBOM
      - name: Sign CycloneDX SBOM
        env:
          COSIGN_EXPERIMENTAL: "true"
        run: |
          cosign sign-blob --yes sbom.cyclonedx.json \
            --bundle sbom.cyclonedx.signed.json

      # 1️⃣1️⃣ Verify SBOM signature
      - name: Verify SBOM signature
        env:
          COSIGN_EXPERIMENTAL: "true"
        run: |
          cosign verify-blob \
            sbom.cyclonedx.json \
            --bundle sbom.cyclonedx.signed.json \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            --certificate-identity-regexp "https://github.com/.*/actions/runs/.*"
        
      # 1️⃣2️⃣ Upload SBOM + signature
      - name: Upload SBOM Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sbom-cyclonedx
          path: |
            sbom.cyclonedx.json
            sbom.cyclonedx.signed.json